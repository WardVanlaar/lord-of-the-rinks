<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lord of the Rinks</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
  <div class="background"></div>
  <div class="wrapper">
    <header>
      <nav>
        {{#if loggedIn}}
         <a href="/">Home</a>
        <a href="/dashboard">Dashboard</a>
        <button id="logout" class="btn-no-style">Logout</button>
        {{else}}
         <a href="/">Home</a>
        <a href="/dashboard">Dashboard</a>
        <a href="/login">Login/Sign-up</a>
        {{/if}}
      </nav>
    </header>
    <main>
      <div class="animate">
        <ul class="list"> 
          <li class="list-item">
            <span class="split-text" data-text="Lord of the Rinks">Lord of the Rinks</span>
          </li>
        </ul>
      </div>
      {{{ body }}}
    </main>
  </div>
  
  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}

  <img src="rideau-canal.jpg" alt="Background" style="background-size:cover;background-attachment:fixed;backgroud-position:80%;z-index:9999;background-image:-moz-element()">
</body>

</html>
{{!-- 
.background {
  background-image: url("../images/rideau-canal.jpg");
  background-size: cover;
  background-attachment: fixed;
  background-position: 80%;
  z-index: 9999;
} --}}
